    /* #### {{ constantCase name }}_RESOLVERS_START #### */
    {{ pascalCase (singular name) }}: {
{{#each schema.fields }}
  {{#if this.resolver}}
      {{@key}}: {{this.resolver}},
  {{else}}
    {{#compare this.type '===' 'User'}}
      {{@key}}: (parent, args, context) =>
        UserQueries.resolveUser(parent, { userId: parent.{{#if parentKey}}{{parentKey}}{{else}}{{@key}}Id{{/if}} }, context),
    {{/compare}}
     {{#compare this.type '===' '[User]'}}
      {{@key}}: (parent, args, context) =>
        UserQueries.users(parent, { _ids: parent.{{#if parentKey}}{{parentKey}}{{else}}{{@key}}Ids{{/if}} }, context),
    {{/compare}}
  {{/if}}
{{/each~}}
    },
    /* #### {{ constantCase name }}_RESOLVERS_END #### */