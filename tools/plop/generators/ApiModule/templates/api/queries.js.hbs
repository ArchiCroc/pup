import {{ pascalCase name }} from './{{ pascalCase name }}';

export default {
  {{ camelCase name }}: (parent, args, context) =>
    context.user && context.user._id ? {{ pascalCase name }}.find().fetch() : [],
  my{{ pascalCase name }}: (parent, args, context) =>
    context.user && context.user._id ? {{ pascalCase name }}.find({ createdById: context.user._id }).fetch() : [],
  {{ camelCase (singular name) }}: (parent, args, context) =>
    {{ pascalCase name }}.findOne({
      $or: [
        { {{ primaryKeyField }}: args.{{ primaryKeyField }}, createdById: context.user && context.user._id ? context.user._id : null },
      //  { {{ primaryKeyField }}: args.{{ primaryKeyField }}, isPublic: true },
      ],
    }),
};
