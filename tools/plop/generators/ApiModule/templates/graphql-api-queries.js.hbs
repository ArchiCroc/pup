      #{{ pascalCase name }}
      {{ camelCase name }}: [{{ pascalCase (singular name) }}]
      my{{ pascalCase name }}: [{{ pascalCase (singular name) }}]
      {{ camelCase (singular name) }}(_id: String): {{ pascalCase (singular name) }}
      