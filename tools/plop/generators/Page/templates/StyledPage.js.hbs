import styled from 'styled-components';
import PageWrapper from '/imports/ui/components/PageWrapper';
const Styled{{pascalCase name}} = styled(PageWrapper)`

`;

export default Styled{{pascalCase name}};
