      #### {{ constantCase pluralName }}_QUERY_TYPES_START ####
      {{ camelCase pluralName }}(
        _ids: [String]
        page: Int
        pageSize: Int
        sort: String
        order: String
        search: String
        {{#each schema.fields}}
          {{#if this.filterable}}
            {{#compare this.type 'in' 'String|Int|Float|Number'}}
        {{ @key }}: [{{#compare this.type '===' 'Number'}}Float{{else}}{{this.type}}{{/compare}}]
             {{else}}
        {{ @key }}: {{#compare this.type '===' 'Number'}}Float{{else}}{{this.type}}{{/compare}}
             {{/compare}}
          {{/if}}
        {{/each}}
      ): {{ pascalCase pluralName }}
      my{{ pascalCase pluralName }}: [{{ pascalCase singularName }}]
      {{ camelCase singularName }}({{ primaryKeyField }}: {{ primaryKeyType }}{{#compare primaryKeyField '!==' urlKeyField }}, {{ urlKeyField }}: {{ urlKeyType }}{{/compare}}): {{ pascalCase singularName }}
      #### {{ constantCase pluralName }}_QUERY_TYPES_END ####