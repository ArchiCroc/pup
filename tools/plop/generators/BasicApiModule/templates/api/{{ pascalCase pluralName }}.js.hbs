/* eslint-disable consistent-return */
import { Mongo } from 'meteor/mongo';

export default new Mongo.Collection('{{ pascalCase pluralName }}'{{#compare primaryKeyType '===' 'ObjectID'}}, { idGeneration: 'MONGO' }{{/compare}});
