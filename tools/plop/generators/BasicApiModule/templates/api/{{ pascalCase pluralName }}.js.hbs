/* eslint-disable consistent-return */
import { Mongo } from 'meteor/mongo';

export default new Mongo.Collection('{{ mongoCollectionCase collectionName }}'{{#compare primaryField.type '===' 'ObjectID'}}, { idGeneration: 'MONGO' }{{/compare}});
