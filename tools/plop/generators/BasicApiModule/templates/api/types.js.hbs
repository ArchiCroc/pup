export default `
type {{ pascalCase singularName }} {
{{#each schema.fields ~}}
  {{~#if this.type}}
  {{@key}}: {{this.type}}
  {{/if~}}
{{~/each}}
}

type {{ pascalCase pluralName }} {
  total: Int
  {{ camelCase pluralName }}: [{{ pascalCase singularName }}]
}

input {{ pascalCase singularName }}Input {
{{#each schema.fields }}
  {{~#compare this.type '&&' this.input}}
  {{@key}}: {{#compare this.type '===' 'Number'}}Float{{else}}{{this.type}}{{/compare}}{{#compare this.validate.optional '==' undefined '||' this.validate.optional '==' false}}{{#compare this.defaultValue '==' undefined}}!{{/compare}}{{/compare}}
  {{/compare~}}
{{/each~}}
}
`;
