      #### {{ constantCase name }}_MUTATION_TYPES_START ####
      save{{ pascalCase (singular name) }}({{ camelCase (singular name) }}: {{ pascalCase (singular name) }}Input): {{ pascalCase (singular name) }}
      remove{{ pascalCase (singular name) }}({{ primaryKeyField }}: {{ primaryKeyType }}!): {{ pascalCase (singular name) }}
      #### {{ constantCase name }}_MUTATION_TYPES_END ####