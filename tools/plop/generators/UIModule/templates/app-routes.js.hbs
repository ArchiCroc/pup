                  {/* #### {{ constantCase name }}_ROUTES_START #### */}
                  <AuthenticatedRoute
                    exact
                    path="/{{ dashCase name }}"
                    component={ {{~ pascalCase name ~}} }
                    setAfterLoginPath={setAfterLoginPath}
                    {...props}
                    {...state}
                  />
                  <AuthenticatedRoute
                    exact
                    path="/{{ dashCase name }}/new"
                    component={New{{ pascalCase (singular name) ~}} }
                    setAfterLoginPath={setAfterLoginPath}
                    {...props}
                    {...state}
                  />
                  <AuthenticatedRoute
                    exact
                    path="/{{ dashCase name }}/:{{primaryKeyField}}"
                    component={View{{ pascalCase (singular name) ~}}}
                    setAfterLoginPath={setAfterLoginPath}
                    {...props}
                    {...state}
                  />
                  <AuthenticatedRoute
                    exact
                    path="/{{ dashCase name }}/:{{primaryKeyField}}/edit"
                    component={Edit{{ pascalCase (singular name) ~}}}
                    setAfterLoginPath={setAfterLoginPath}
                    {...props}
                    {...state}
                  />
                  {/* #### {{ constantCase name }}_ROUTES_END #### */}