#import "../fragments/{{ pascalCase name }}.gql"

query {{ camelCase (singular name) }} {
  {{ camelCase (singular name) }} {
    ...{{ pascalCase (singular name) }}Attributes
  }
}

query edit{{ pascalCase (singular name) }}(${{ primaryKeyField }}: String) {
  {{ camelCase (singular name) }}({{ primaryKeyField }}: ${{ primaryKeyField }}) {
    ...{{ pascalCase (singular name) }}Attributes
  }
}

query {{ camelCase (singular name) }}(${{ primaryKeyField }}: String) {
  {{ camelCase (singular name) }}({{ primaryKeyField }}: ${{ primaryKeyField }}) {
    ...{{ pascalCase (singular name) }}Attributes
  }
}
