import React from 'react';
import PropTypes from 'prop-types';
import i18n from 'meteor/universe:i18n';
import Button from 'antd/lib/button';
import { LinkContainer } from 'react-router-bootstrap';

const New{{ pascalCase (singular name) }}Button = (props) => (
  <LinkContainer to="/{{ dashCase name }}/new">
    <Button type="primary" icon="plus" {...props} >
      {i18n.__('{{ pascalCase name }}.new_{{ snakeCase (singular name) }}')}
    </Button>
  </LinkContainer>
);

New{{ pascalCase (singular name) }}Button.defaultProps = {
  className: undefined,
  disabled: false,
};

New{{ pascalCase (singular name) }}Button.propTypes = {
  className: PropTypes.string,
  disabled: PropTypes.bool,
};

export default New{{ pascalCase (singular name) }}Button;
