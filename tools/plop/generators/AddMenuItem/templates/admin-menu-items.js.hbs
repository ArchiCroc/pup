      {{#compare menu '===' 'admin'}}
      <Menu.Item key="/{{ dashCase moduleName }}/{{ urlSlug }}">
        {i18n.__('{{ pascalCase moduleName }}.{{ snakeCase name ~}}')}
        <Link to="/{{ moduleName }}/{{ urlSlug }}" />
      </Menu.Item>
      {{/compare}}