          <Menu.Item key="{{ uiRouteBasePath }}{{#if urlSlug}}/{{ urlSlug }}{{/if}}">
            {i18n.__('{{ pascalCase pluralName }}.{{ snakeCase label }}')}
            <Link to="{{ uiRouteBasePath }}{{#if urlSlug}}/{{ urlSlug }}{{/if}}" />
          </Menu.Item>