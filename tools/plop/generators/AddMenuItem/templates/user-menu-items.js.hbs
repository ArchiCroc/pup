{{#compare menuItemType '===' 'user'}}
      <Menu.Item key="/{{ uiFolderName }}/{{ urlSlug }}">
        {i18n.__('{{ pascalCase uiFolderName }}.{{ snakeCase name ~}}')}
        <Link to="/{{ uiFolderName }}/{{ urlSlug }}" />
      </Menu.Item>
{{/compare}}
