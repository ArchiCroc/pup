  //  {{queryType}}
  {{queryName}}: (parent, args, context) => {
    if (!context.user) {
      throw new Error('Sorry, you must be logged in to {{queryName}} {{moduleName}}');
    }
    // if (!isAdmin(context.user._id)) {
    //   throw new Error('[Sorry, you must be an Admin to query {{queryName}} {{moduleName}}');
    // }

    const query = {};
    
    return ProjectLocations.find(query, { sort: { name: 1 } });
    // .map((item) => {
    //  item._id = item._id._str;
    //  return item;
    // });
  },