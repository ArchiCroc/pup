import createIndex from '{{apiPathOffset}}../../../modules/server/createIndex';
import {{ pascalCase pluralName }} from '{{apiPathOffset}}../{{ pascalCase pluralName }}';

createIndex({{ pascalCase pluralName }}, { owner: 1 });
