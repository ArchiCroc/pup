import React, { useState } from 'react';
import PropTypes from 'prop-types';
import i18n from 'meteor/universe:i18n';
import PageBreadcrumbs, { Breadcrumb } from '{{uiPathOffset}}../components/PageBreadcrumbs';
import PageHeader from '{{uiPathOffset}}../components/PageHeader';
import {{ pascalCase pluralName }}Table from './components/{{ pascalCase pluralName }}Table';

import Styled{{ pascalCase pluralName }} from './Styled{{ pascalCase pluralName }}';

function {{ pascalCase pluralName }}({ roles }) {
  return (
    <Styled{{ pascalCase pluralName }}{{#compare schema.indexWidth '===' 'full'}} md={24} lg={24} xl={24}{{/compare}}>
      <PageBreadcrumbs>
        <Breadcrumb>{i18n.__('{{ pascalCase pluralName }}.{{ snakeCase shortSingularName }}_plural')}</Breadcrumb>
      </PageBreadcrumbs>
      <PageHeader title={i18n.__('{{ pascalCase pluralName }}.{{ snakeCase shortSingularName }}_plural')} />
      <{{ pascalCase pluralName }}Table roles={roles} showNew{{ pascalCase singularName }}Button />
    </Styled{{ pascalCase pluralName }}>
  );
}

{{ pascalCase pluralName }}.propTypes = {
  roles: PropTypes.array.isRequired,
};

export default {{ pascalCase pluralName }};
