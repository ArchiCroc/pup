import styled from 'styled-components';
import PageWrapper from '{{uiPathOffset}}../components/PageWrapper';

const Styled{{ pascalCase pluralName }}Page = styled(PageWrapper)``;
export default Styled{{ pascalCase pluralName }}Page;
