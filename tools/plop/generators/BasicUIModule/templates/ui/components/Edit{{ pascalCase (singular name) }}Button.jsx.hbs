import React from 'react';
import PropTypes from 'prop-types';
import i18n from 'meteor/universe:i18n';
import Button from 'antd/lib/button';
import { LinkContainer } from 'react-router-bootstrap';

const Edit{{ pascalCase (singular name) }}Button = ({ {{ urlKeyField }}, className, disabled }) => (
  <LinkContainer to={`/{{ dashCase name }}/${ {{~ urlKeyField ~}} }/edit`}>
    <Button type="primary" icon="plus" className={className}>
      {i18n.__('{{ pascalCase name }}.edit_{{ snakeCase (singular name) }}')}
    </Button>
  </LinkContainer>
);

Edit{{ pascalCase (singular name) }}Button.defaultProps = {
  className: undefined,
  disabled: false,
};

Edit{{ pascalCase (singular name) }}Button.propTypes = {
  className: PropTypes.string,
  disabled: PropTypes.bool,
  {{ urlKeyField }}: PropTypes.string.isRequired,
};

export default Edit{{ pascalCase (singular name) }}Button;
