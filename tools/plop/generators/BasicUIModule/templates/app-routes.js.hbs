                  {/* #### {{ constantCase pluralName }}_ROUTES_START #### */}
                  <AuthenticatedRoute
                    exact
                    path="/{{ uiFolderName }}"
                    component={ {{~ pascalCase pluralName ~}} }
                    setAfterLoginPath={setAfterLoginPath}
                    {...props}
                    {...state}
                  />
                  <AuthenticatedRoute
                    exact
                    path="/{{ uiFolderName }}/new"
                    component={New{{ pascalCase singularName ~}} }
                    setAfterLoginPath={setAfterLoginPath}
                    {...props}
                    {...state}
                  />
                  <AuthenticatedRoute
                    exact
                    path="/{{ uiFolderName }}/:{{urlKeyField}}"
                    component={View{{ pascalCase singularName ~}}}
                    setAfterLoginPath={setAfterLoginPath}
                    {...props}
                    {...state}
                  />
                  <AuthenticatedRoute
                    exact
                    path="/{{ uiFolderName }}/:{{urlKeyField}}/edit"
                    component={Edit{{ pascalCase singularName ~}}}
                    setAfterLoginPath={setAfterLoginPath}
                    {...props}
                    {...state}
                  />
                  {/* #### {{ constantCase pluralName }}_ROUTES_END #### */}